import { Channels } from "./channels";
export interface FootwearCreatedEvent {
    channel: Channels.FootwearCreated;
    data: {
        id: string;
        version: number;
        title: string;
        price: number;
        userId: string;
    };
}
