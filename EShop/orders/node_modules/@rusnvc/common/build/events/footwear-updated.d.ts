import { Channels } from "./channels";
export interface FootwearUpdatedEvent {
    channel: Channels.FootwearUpdated;
    data: {
        id: string;
        version: number;
        title: string;
        price: number;
        userId: string;
        orderId?: string;
    };
}
