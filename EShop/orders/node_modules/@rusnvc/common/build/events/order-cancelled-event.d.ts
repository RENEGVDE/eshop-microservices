import { Channels } from "./channels";
export interface OrderCancelledEvent {
    channel: Channels.OrderCancelled;
    data: {
        id: string;
        version: number;
        footwear: {
            id: string;
        };
    };
}
