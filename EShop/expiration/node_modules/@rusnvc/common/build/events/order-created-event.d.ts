import { Channels } from "./channels";
import { OrderStatus } from "./types/order-status";
export interface OrderCreatedEvent {
    channel: Channels.OrderCreated;
    data: {
        id: string;
        version: number;
        status: OrderStatus;
        userId: string;
        expiresAt: string;
        footwear: {
            id: string;
            price: number;
        };
    };
}
